-- List all records in second_table.
-- Select all score, name records from second_table and order
-- resulting table by Descending scores
SELECT score, name
        FROM second_table
        ORDER BY score DESC;
